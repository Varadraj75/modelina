asyncapi: 2.6.0
info:
  title: Circular Array Crash Repro
  version: 1.0.0

channels:
  test/channel:
    publish:
      message:
        payload:
          type: array
          items:
            - $ref: '#/components/schemas/Node'
            - $ref: '#/components/schemas/Node'

components:
  schemas:
    Node:
      type: object
      properties:
        children:
          type: array
          items:
            - $ref: '#/components/schemas/Node'
